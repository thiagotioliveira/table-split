spring:
  application:
    name: table-split
  datasource:
    url: jdbc:h2:mem:table-split
  messages:
    basename: messages
    encoding: UTF-8

cloudinary:
  cloud-name:
  api-key:
  api-secret:
  root-folder: dev

logging:
  level:
    ROOT: INFO
    dev.thiagooliveira: DEBUG